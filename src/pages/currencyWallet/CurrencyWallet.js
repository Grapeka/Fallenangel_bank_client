import classes from "./currencyWallet.module.scss";

const CurrencyWallet = () => {
  return (
    <div className={classes.container}>
      <div className={classes.insideContainer}></div>
    </div>
  );
};

export default CurrencyWallet;
